commit 27565255e1548a34e618f878adaea92255236f65
Author: Your Name <you@example.com>
Date:   Fri Jul 10 08:35:31 2026 +0000

    update device selection

diff --git a/ubs-virt-enpu/vcann-rt/src/ascend/device.c b/ubs-virt-enpu/vcann-rt/src/ascend/device.c
index d13bafc..1ede0b0 100644
--- a/ubs-virt-enpu/vcann-rt/src/ascend/device.c
+++ b/ubs-virt-enpu/vcann-rt/src/ascend/device.c
@@ -49,34 +49,19 @@ static int validate_and_set_device_context(int devId, int *device_index)
     int current_active = get_active_device_id();
     if (current_active >= 0 && current_active < device_count) {
         // Use the current active device if the requested devId is not specifically for a different device
-        if (devId == 0 || devId == get_device_id()) {
-            *device_index = current_active;
-            return ACL_RT_SUCCESS;
-        }
+        LOG_INFO("Found active device index %d", current_active);
+       	*device_index = current_active;
+        return ACL_RT_SUCCESS;
     }
-    
-    // Find device by requested physical NPU ID if provided
-    if (devId != 0) {
-        // Get all device configurations to find the one matching devId
-        for (int i = 0; i < device_count; i++) {
-            npu_info info;
-            if (get_device_info_by_index(i, &info) == ENPU_SUCCESS) {
-                // Check if this device's logic_id matches the requested devId
-                if (info.logic_id == devId) {
-                    set_active_device_id(i);
-                    *device_index = i;
-                    LOG_DEBUG("Found and set active device index %d (logic ID %d) for requested devId %d", 
-                             i, devId, devId);
-                    return ACL_RT_SUCCESS;
-                }
-            }
-        }
+
+    if (devId >= device_count) {
+    	LOG_ERROR("Invalid device ID: %d", devId);
+	return ACL_ERROR_INVALID_PARAM;
     }
     
-    // Default to first device if devId=0 or not found
-    *device_index = 0;
-    set_active_device_id(0);
-    LOG_INFO("DevId %d not found, using default device index %d", devId, *device_index);
+    *device_index = devId;
+    set_active_device_id(devId);
+    LOG_INFO("Found and set active device index %d", devId);
     return ACL_RT_SUCCESS;
 }
 
diff --git a/ubs-virt-enpu/vcann-rt/src/ascend/memory.c b/ubs-virt-enpu/vcann-rt/src/ascend/memory.c
index 52ec461..3877ac7 100644
--- a/ubs-virt-enpu/vcann-rt/src/ascend/memory.c
+++ b/ubs-virt-enpu/vcann-rt/src/ascend/memory.c
@@ -83,6 +83,7 @@ RUNTIME_HOOK_DEFINE(rtMallocPhysical, rtDrvMemHandle *handle, size_t size, rtDrv
     if (ret != ENPU_SUCCESS) {
         return ret;
     }
+    prop->devid = get_logic_id();
     return RUNTIME_HOOK_CALL(rt_library_entry, rtMallocPhysical, handle, size, prop, flags);
 }
 
@@ -106,4 +107,4 @@ RUNTIME_HOOK_DEFINE(rtMemGetInfoEx, rtMemInfoType_t memInfoType, size_t *freeSiz
     *freeSize = remain;
     *totalSize = quota;
     return RT_ERROR_NONE;
-}
\ No newline at end of file
+}
