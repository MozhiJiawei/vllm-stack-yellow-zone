diff --git a/src/include/config.h b/src/include/config.h
index 306eded..c4d5981 100644
--- a/src/include/config.h
+++ b/src/include/config.h
@@ -63,7 +63,6 @@ int load_config(const char *file_path);
 int load_multi_config(const char *file_path); // Load multi-device config
 int get_device_count(void);                // Get number of configured devices
 int get_device_config(int index, struct ConfigSection *config); // Get device config by index
-int find_device_section(int phy_npu_id);   // Find section by physical NPU ID
 void reset_single_config(void);            // Reset single device config
 void reset_multi_config(void);             // Reset multi-device config
 
diff --git a/src/include/npu_manager.h b/src/include/npu_manager.h
index 96c8b06..9bcedc8 100644
--- a/src/include/npu_manager.h
+++ b/src/include/npu_manager.h
@@ -74,27 +74,14 @@ typedef struct npu_info {
     uint8_t soc_version;
 } npu_info;
 
-// Thread-local device context for multi-NPU support
-#ifdef __GNUC__
-#define THREAD_LOCAL __thread
-#else
-#define THREAD_LOCAL thread_local
-#endif
-
 // Global variables
 extern void enpu_global_init(void);
 extern void enpu_global_init_post(void);
 
 // Multiple NPU device support
-extern int get_device_count(void);
-extern int get_active_device_id(void);
-extern void set_active_device_id(int device_id);
-extern int get_device_info_by_phy_id(int phy_npu_id, npu_info *info);
-extern int get_device_info_by_index(int device_index, npu_info *info);
-
-// Device-specific scheduler context
-extern vnpu_time_slice_sched_t* get_current_sched_context(void);
-extern vnpu_time_slice_sched_t* get_sched_context_by_index(int device_index);
+int get_active_device_id(void);
+void set_active_device_id(int device_id);
+int get_device_info_by_index(int device_index, npu_info *info);
 
 // Backward compatibility functions (operate on active device)
 extern bool is_core_limit(void);
@@ -121,12 +108,10 @@ extern struct npu_info g_npu_info_array[MAX_NPU_DEVICES];
 int get_mem_used(size_t *used);
 
 // Multi-device initialization functions
-extern int enpu_load_config(void);
-extern int enpu_load_multi_config(void);
-extern int get_device_config_count(void);
-extern int enpu_init_devices(void);
-extern int enpu_device_init(int phy_npu_id);
-extern int enpu_config_info(int phy_npu_id);
+int enpu_load_config(void);
+int enpu_load_multi_config(void);
+int enpu_device_init(int phy_npu_id);
+int enpu_config_info(int phy_npu_id);
 extern int enpu_soc_init(void);
 #if defined(__cplusplus)
 }
diff --git a/src/npu_manager.c b/src/npu_manager.c
index 814eb09..8628483 100644
--- a/src/npu_manager.c
+++ b/src/npu_manager.c
@@ -35,25 +35,6 @@ static pthread_once_t post_init_flag = PTHREAD_ONCE_INIT;
 // Per-device scheduler context pointers
 static vnpu_time_slice_sched_t* g_device_sched_contexts[MAX_NPU_DEVICES];
 
-// Get scheduler context for the currently active device
-vnpu_time_slice_sched_t* get_current_sched_context(void)
-{
-    int idx = get_active_device_id();
-    if (idx >= 0 && idx < MAX_NPU_DEVICES) {
-        return g_device_sched_contexts[idx];
-    }
-    return NULL;
-}
-
-// Get scheduler context for a specific device index
-vnpu_time_slice_sched_t* get_sched_context_by_index(int device_index)
-{
-    if (device_index >= 0 && device_index < MAX_NPU_DEVICES) {
-        return g_device_sched_contexts[device_index];
-    }
-    return NULL;
-}
-
 // Thread-local context management
 int get_active_device_id(void)
 {
@@ -172,48 +153,6 @@ bool check_init_success(void)
     return (idx >= 0 && idx < MAX_NPU_DEVICES) ? g_npu_info_array[idx].initialization : false;
 }
 
-// Multi-device functions
-//int get_device_count(void)
-//{
-//    return get_device_config_count();
-//}
-
-int get_device_config_count(void)
-{
-    int count = 0;
-    for (int i = 0; i < MAX_NPU_DEVICES; i++) {
-        if (multi_config.sections[i].phy_npu_id != INVALID_VALUE) {
-            count++;
-        }
-    }
-    return count;
-}
-
-int get_device_info_by_phy_id(int phy_npu_id, npu_info *info)
-{
-    if (!info) {
-        LOG_ERROR("Invalid info parameter");
-        return ENPU_FAIL;
-    }
-    
-    // Find the device index with the specified physical NPU ID
-    int device_index = -1;
-    for (int i = 0; i < MAX_NPU_DEVICES; i++) {
-        if (g_npu_info_array[i].pnpu_id == phy_npu_id) {
-            device_index = i;
-            break;
-        }
-    }
-    
-    if (device_index == -1) {
-        LOG_ERROR("Device not found for physical NPU ID: %d", phy_npu_id);
-        return ENPU_FAIL;
-    }
-    
-    *info = g_npu_info_array[device_index];
-    return ENPU_SUCCESS;
-}
-
 int get_device_info_by_index(int device_index, npu_info *info)
 {
     if (!info || device_index < 0 || device_index >= MAX_NPU_DEVICES) {
@@ -363,7 +302,6 @@ int enpu_device_init(int phy_npu_id)
 
     LOG_INFO("Got device_index %d for phy npu id %d", device_index, phy_npu_id);
 
-    enpu_soc_init();
     // Get device information from DCMI
     int card_id = -1, device_id = -1, logic_id = -1;
     int rc = enpu_dcmi_get_card_info(npu->pnpu_id, &card_id, &device_id, &logic_id, npu->soc_version);
@@ -442,7 +380,7 @@ int enpu_load_config(void)
     // Check if we loaded a single-device or multi-device config
     if (multi_config.device_count > 0) {
         LOG_INFO("Loaded multi-device configuration with %d devices", multi_config.device_count);
-        return enpu_init_devices();
+        return ENPU_SUCCESS;
     } else if (config.phy_npu_id != INVALID_VALUE) {
         LOG_INFO("Loaded single-device configuration for physical NPU ID %d", config.phy_npu_id);
         return enpu_config_info(config.phy_npu_id);
@@ -475,6 +413,17 @@ static void __enpu_global_init(void)
     rc = enpu_soc_init();
     CHECK_COND_RETURN(rc != ENPU_SUCCESS, "Failed to initialize SOC.");
 
+    if (multi_config.device_count > 0) {
+        rc = enpu_init_devices();
+        CHECK_COND_RETURN(rc != ENPU_SUCCESS, "Failed to initialize enpu devices.");
+    } else {
+        rc = enpu_device_init(config.phy_npu_id);
+        CHECK_COND_RETURN(rc != ENPU_SUCCESS, "Failed to initialize enpu device.");
+    }
+
+    rc = memory_limiter_init();
+    CHECK_COND_RETURN(rc != ENPU_SUCCESS, "Failed to initialize memory limiter");
+
     LOG_INFO("Global NPU initialization completed");
 }
 
diff --git a/src/utils/config.c b/src/utils/config.c
index 6cb3138..70ccc4d 100644
--- a/src/utils/config.c
+++ b/src/utils/config.c
@@ -323,37 +323,4 @@ int load_multi_config(const char *file_path)
 int get_device_count(void)
 {
     return multi_config.device_count;
-}
-
-int get_device_config(int index, struct ConfigSection *config_out)
-{
-    if (index < 0 || index >= multi_config.device_count || !config_out) {
-        LOG_ERROR("Invalid parameters for get_device_config: index=%d, config_out=%p", 
-                 index, config_out);
-        return ENPU_FAIL;
-    }
-    
-    // Find the valid section at index
-    int valid_count = 0;
-    for (int i = 0; i < MAX_NPU_DEVICES; i++) {
-        if (multi_config.sections[i].phy_npu_id != INVALID_VALUE) {
-            if (valid_count == index) {
-                *config_out = multi_config.sections[i];
-                return ENPU_SUCCESS;
-            }
-            valid_count++;
-        }
-    }
-    
-    return ENPU_FAIL;
-}
-
-int find_device_section(int phy_npu_id)
-{
-    for (int i = 0; i < MAX_NPU_DEVICES; i++) {
-        if (multi_config.sections[i].phy_npu_id == phy_npu_id) {
-            return i;
-        }
-    }
-    return -1;
-}
+}
\ No newline at end of file
