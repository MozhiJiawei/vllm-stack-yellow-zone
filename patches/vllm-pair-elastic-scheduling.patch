diff --git a/vllm/v1/executor/uniproc_executor.py b/vllm/v1/executor/uniproc_executor.py
index b616c3b7b..3a76a97ac 100644
--- a/vllm/v1/executor/uniproc_executor.py
+++ b/vllm/v1/executor/uniproc_executor.py
@@ -23,9 +23,46 @@ from vllm.v1.worker.worker_base import WorkerWrapperBase
 logger = init_logger(__name__)
 
 
+def _install_pair_worker_gate(worker, vllm_config):
+    """Install the gate only when enabled, leaving MODE=off's hot path native."""
+    if os.environ.get("VLLM_PAIR_SCHED_MODE", "off").lower() == "off":
+        return None
+
+    parallel_config = vllm_config.parallel_config
+    if parallel_config.pipeline_parallel_size != 1:
+        raise ValueError("vLLM pair scheduling v3 does not support Pipeline Parallel")
+    if parallel_config.nnodes_within_dp != 1:
+        raise ValueError("vLLM pair scheduling v3 requires single-node workers")
+    if parallel_config.world_size != 1:
+        raise ValueError(
+            "vLLM pair scheduling v3 UniProc integration requires world size 1"
+        )
+
+    from vllm_pair_scheduler import create_worker_forward_gate_from_env
+
+    gate = create_worker_forward_gate_from_env(worker_rank=0, worker_count=1)
+    execute_model = worker.execute_model
+
+    def gated_execute_model(scheduler_output):
+        if scheduler_output.total_num_scheduled_tokens <= 0:
+            return execute_model(scheduler_output)
+        forward_round = gate.enter_forward()
+        try:
+            output = execute_model(scheduler_output)
+            gate.leave_forward(*forward_round)
+            return output
+        except Exception:
+            gate.fail(201)
+            raise
+
+    worker.execute_model = gated_execute_model
+    return gate
+
+
 class UniProcExecutor(Executor):
     def _init_executor(self) -> None:
         """Initialize the worker and load the model."""
+        self._pair_forward_gate = None
         self.driver_worker = WorkerWrapperBase(rpc_rank=0)
         distributed_init_method, rank, local_rank = self._distributed_args()
         kwargs = dict(
@@ -51,6 +88,9 @@ class UniProcExecutor(Executor):
         else:
             self.driver_worker.load_model()
         current_platform.update_block_size_for_backend(self.vllm_config)
+        self._pair_forward_gate = _install_pair_worker_gate(
+            self.driver_worker, self.vllm_config
+        )
 
     def _distributed_args(self) -> tuple[str, int, int]:
         """Return (distributed_init_method, rank, local_rank)."""
@@ -133,6 +173,9 @@ class UniProcExecutor(Executor):
         return
 
     def shutdown(self) -> None:
+        if self._pair_forward_gate is not None:
+            self._pair_forward_gate.close()
+            self._pair_forward_gate = None
         if worker := self.driver_worker:
             worker.shutdown()
 
diff --git a/vllm/v1/executor/multiproc_executor.py b/vllm/v1/executor/multiproc_executor.py
index f9b771540..5a552eef0 100644
--- a/vllm/v1/executor/multiproc_executor.py
+++ b/vllm/v1/executor/multiproc_executor.py
@@ -64,6 +64,46 @@ from vllm.v1.worker.worker_base import WorkerWrapperBase
 logger = init_logger(__name__)
 
 
+def _install_pair_worker_gate(
+    worker: WorkerWrapperBase, vllm_config: VllmConfig, rank: int
+):
+    """Install the gate only when enabled, leaving MODE=off's hot path native."""
+    if os.environ.get("VLLM_PAIR_SCHED_MODE", "off").lower() == "off":
+        return None
+
+    parallel_config = vllm_config.parallel_config
+    if parallel_config.pipeline_parallel_size != 1:
+        raise ValueError("vLLM pair scheduling v3 does not support Pipeline Parallel")
+    if parallel_config.nnodes_within_dp != 1:
+        raise ValueError("vLLM pair scheduling v3 requires single-node workers")
+    worker_count = parallel_config.local_world_size
+    if worker_count > 64:
+        raise ValueError("vLLM pair scheduling v3 supports at most 64 local workers")
+
+    from vllm_pair_scheduler import create_worker_forward_gate_from_env
+
+    gate = create_worker_forward_gate_from_env(
+        worker_rank=rank % worker_count,
+        worker_count=worker_count,
+    )
+    execute_model = worker.execute_model
+
+    def gated_execute_model(scheduler_output):
+        if scheduler_output.total_num_scheduled_tokens <= 0:
+            return execute_model(scheduler_output)
+        forward_round = gate.enter_forward()
+        try:
+            output = execute_model(scheduler_output)
+            gate.leave_forward(*forward_round)
+            return output
+        except Exception:
+            gate.fail(201)
+            raise
+
+    worker.execute_model = gated_execute_model
+    return gate
+
+
 class FutureWrapper(Future):
     def __init__(
         self,
@@ -581,6 +621,7 @@ class WorkerProc:
         is_driver_worker: bool,
     ):
         self.rank = rank
+        self._pair_forward_gate = None
         wrapper = WorkerWrapperBase(rpc_rank=local_rank, global_rank=rank)
         # TODO: move `init_worker` to executor level as a collective rpc call
         all_kwargs: list[dict] = [
@@ -629,6 +670,9 @@ class WorkerProc:
         # Initialize message queues after init_device() since multi-node setups
         # (nnodes_within_dp > 1) require distributed groups to be initialized
         self._init_message_queues(input_shm_handle, vllm_config)
+        self._pair_forward_gate = _install_pair_worker_gate(
+            self.worker, vllm_config, rank
+        )
 
         # Enable environment variable cache (e.g. assume no more
         # environment variable overrides after this point)
@@ -742,6 +786,9 @@ class WorkerProc:
         return cast(list[WorkerProcHandle], ready_proc_handles)
 
     def shutdown(self):
+        if self._pair_forward_gate is not None:
+            self._pair_forward_gate.close()
+            self._pair_forward_gate = None
         if self.rpc_broadcast_mq is not None:
             self.rpc_broadcast_mq.shutdown()
         if self.worker_response_mq is not None:
